

1. 图中，m=0-5。一共 6 个组，

2. 每个组都可以把 0~n，n=31 个 PPI 通道包含到其中。那么包含到一个 group 组内的 PPI 通道就可以进行统一的管理与操作。比如打开或者关掉 PPI 通道。
   例如CHG0.2-0=111, 表示把PPI通道0，1，2绑定到PPI通道组0

3. 通过寄存器 CHG [n] .EN 和 CHG [n] .DIS 来打开或者关掉包含在组里的所有 PPI 通道。

4. PPI 组事件 CHG [0] .EN 也可以像任何其他任务一样通过 PPI 触发，这意味着它们可以作为 TEP 连接到 PPI 通道，因此可以通过 PPI 事件来管理一个 group 组

5.  当一个通道属于两个组 m 和 n，并且 CHG [m].EN 和 CHG [n].DIS 同时发生时（m 和 n 可以相等或不同），该通道上的 EN 具有优先权。





